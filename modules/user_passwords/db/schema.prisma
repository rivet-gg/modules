// Do not modify this `datasource` block
datasource db {
	provider = "postgresql"
	url      = env("DATABASE_URL")
}

model Passwords {
    userId          String      @db.Uuid @id
	passwordHash    String
	algo            String
}
