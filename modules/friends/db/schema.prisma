datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Friend {
    userIdA         String        @db.Uuid
    userIdB         String        @db.Uuid
    createdAt       DateTime      @default(now())
    removedAt       DateTime?

    @@id([userIdA, userIdB])
}
