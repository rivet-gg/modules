{
  "name": "open-game-services",
  "private": true,
  "packageManager": "yarn@4.0.2",
  "scripts": {
    "dev": "nx watch --all -- nx affected -t dev --files \\$NX_FILE_CHANGES"
  },
  "workspaces": [
    "engine",
    "modules/*"
  ],
  "devDependencies": {
    "nx": "18.0.4"
  }
}
