registries:
  default:
    git:
      url: https://github.com/rivet-gg/open-game-services.git
      branch: main
      directory: ./modules
  # default:
  #   local:
  #       directory: ../../../opengb-registry/modules
  local:
    local:
        directory: ./modules
modules:
  # currency: {}
  # friends: {}
  # rate_limit: {}
  # tokens: {}
  # users: {}
  # auth:
  #   config:
  #     google:
  #       token: abc123
  #     discord:
  #       token: abc123
  foo:
    registry: local
