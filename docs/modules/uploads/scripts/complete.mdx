---
title: "Complete Upload"
description: "Alert the module that the upload has been completed"
sidebarTitle: "Complete Upload"
---

| Source                                                                                                      | Name       | Public     |
| ----------------------------------------------------------------------------------------------------------- | ---------- | ---------- |
| [Source](https://github.com/rivet-gg/opengb-registry/tree/main/modules/uploads/scripts/complete.ts) | `complete` | No |

<RequestExample>
```typescript OpenGB Script
const data = await ctx.modules.uploads.complete({
  // Request body
});
```

</RequestExample>

## Request

```typescript
export interface Request {
  uploadId: string;
}
```

## Response

```typescript
export interface Response {
  upload: Upload;
}
export interface Upload {
  id: string;
  userId: null | string;
  bucket: string;
  contentLength: string;
  files?: UploadFile[];
  createdAt: string;
  updatedAt: string;
  completedAt: null | string;
}
export interface UploadFile {
  path: string;
  mime: null | string;
  contentLength: string;
}
```
