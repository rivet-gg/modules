---
title: "Get Upload Metadata"
description: "Get the metadata (including contained files) for specified upload IDs"
sidebarTitle: "Get Upload Metadata"
---

| Source                                                                                                      | Name       | Public     |
| ----------------------------------------------------------------------------------------------------------- | ---------- | ---------- |
| [Source](https://github.com/rivet-gg/opengb-registry/tree/main/modules/uploads/scripts/get.ts) | `get` | No |

<RequestExample>
```typescript OpenGB Script
const data = await ctx.modules.uploads.get({
  // Request body
});
```

</RequestExample>

## Request

```typescript
export interface Request {
  uploadIds: string[];
  includeFiles?: boolean;
}
```

## Response

```typescript
export interface Response {
  uploads: UploadWithOptionalFiles[];
}
export interface UploadWithOptionalFiles {
  userId: null | string;
  id: string;
  createdAt: string;
  updatedAt: string;
  bucket: string;
  contentLength: string;
  completedAt: null | string;
  files?: UploadFile[];
}
export interface UploadFile {
  path: string;
  mime: null | string;
  contentLength: string;
}
```
