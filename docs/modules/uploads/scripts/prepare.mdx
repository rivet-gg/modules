---
title: "Prepare Upload"
description: "Prepare an upload batch for data transfer"
sidebarTitle: "Prepare Upload"
---

| Source                                                                                                      | Name       | Public     |
| ----------------------------------------------------------------------------------------------------------- | ---------- | ---------- |
| [Source](https://github.com/rivet-gg/opengb-registry/tree/main/modules/uploads/scripts/prepare.ts) | `prepare` | No |

<RequestExample>
```typescript OpenGB Script
const data = await ctx.modules.uploads.prepare({
  // Request body
});
```

</RequestExample>

## Request

```typescript
export interface Request {
  userId?: string;
  files: {
    path: string;
    mime: null | string;
    contentLength: string;
    multipart: boolean;
  }[];
}
```

## Response

```typescript
export interface Response {
  upload: PresignedUpload;
}
export interface PresignedUpload {
  files: PresignedUploadFile[];
  userId: null | string;
  id: string;
  createdAt: string;
  updatedAt: string;
  bucket: string;
  contentLength: string;
  completedAt: null | string;
}
export interface PresignedUploadFile {
  presignedChunks: PresignedChunk[];
  path: string;
  mime: null | string;
  contentLength: string;
}
export interface PresignedChunk {
  url: string;
  partNumber: number;
  contentLength: string;
  offset: string;
}
```
