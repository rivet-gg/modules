---
title: "Throttle Public"
description: "Limit the amount of times a public request can be made by a given key. This will rate limit based off the user's IP address."
sidebarTitle: "Throttle Public"
---

| Source                                                                                                      | Name       | Public     |
| ----------------------------------------------------------------------------------------------------------- | ---------- | ---------- |
| [Source](https://github.com/rivet-gg/opengb-modules/tree/main/modules/rate_limit/scripts/throttle_public.ts) | `throttle_public` | No |

<RequestExample>
```typescript OpenGB Script
const data = await ctx.modules.rateLimit.throttlePublic({
  // Request body
});
```

</RequestExample>

## Request

```typescript
export interface Request {
  /**
   * Number of requests in `period` before rate limiting.
   */
  requests?: number;
  /**
   * How frequently to reset the request counter.
   */
  period?: number;
}
```

## Response

```typescript
export interface Response {}
```
