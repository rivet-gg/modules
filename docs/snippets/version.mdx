export const Version = () => {
  const version = "0.1.5";
  return (
    <CodeBlock>{`deno install -n opengb -fgA https://raw.githubusercontent.com/rivet-gg/opengb/v${version}/src/cli/main.ts`}</CodeBlock>
  )
};
